import { Injectable } from "@nestjs/common";

@Injectable()
export class SignService {
  // TODO: 확정 수화 인식 결과를 DB에 저장
  // async saveSignCaption(input: {
  //   roomId: string;
  //   sessionId: string;
  //   content: string;
  //   confidence: number;
  // }) { ... }
}
